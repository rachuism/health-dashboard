{
  "name": "health-dashboard",
  "private": true,
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^22.0.0"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test parse.test.js"
  }
}
